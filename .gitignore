# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Tooling caches
.pytest_cache/
.ruff_cache/

# Runtime artifacts (checkpoints, server logs)
.checkpoints/
output/

# Secrets & local environment — never commit; .env.example is the template
.env

# azd environment state (subscription id, resource ids, deployed-agent state)
.azure/

# macOS
.DS_Store
